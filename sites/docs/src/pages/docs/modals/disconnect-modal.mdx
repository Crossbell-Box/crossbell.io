import { Callout } from "nextra-theme-docs";
import { OptionTable } from "@/components";
import { DisconnectModalExample } from "@/examples";

# Disconnect Modal

<div className="my-8">
	<DisconnectModalExample />
</div>

<Callout>
	Note: It is recommended to use [ConnectButton](/docs/connect-button) to render
	the connect/disconnect button.
</Callout>

## Usage

```tsx
import { useDisconnectModal } from "@crossbell/connet-kit";

export const Main = () => {
	const { isActive, show, hide } = useDisconnectModal();

	return <button onClick={show}>Disconnect</button>;
};
```

## Return type

<OptionTable
	options={[
		["isActive", "boolean", "Indicates whether the modal is activated or not"],
		["show", "() => void", "Show modal"],
		["hide", "() => void", "Hide modal"],
	]}
/>
