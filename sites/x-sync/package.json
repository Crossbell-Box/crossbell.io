{
	"name": "x-sync",
	"version": "0.0.8",
	"private": true,
	"scripts": {
		"build": "rm -rf .next && next build",
		"dev": "rm -rf .next && next dev -p 3001",
		"lint": "next lint",
		"start": "next start"
	}
}
